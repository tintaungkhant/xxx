enum OddType {
    Myanmar = "myanmar",
    Malay = "malay"
}

enum SiteName {
    ibet789 = "ibet789",
}

enum TeamType {
    Home = "home",
    Away = "away"
}

export {
    OddType,
    SiteName,
    TeamType
}